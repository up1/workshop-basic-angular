import { ReplaceSpaceWithDashPipe } from './replace-space-with-dash.pipe';

describe('ReplaceSpaceWithDashPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpaceWithDashPipe();
    expect(pipe).toBeTruthy();
  });
});
